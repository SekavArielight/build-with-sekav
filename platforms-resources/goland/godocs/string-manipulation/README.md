# The strings package contains many useful functions to work on strings.

- Strings in Go are immutable. These functions do not modify the original string; they return a new copy with the changes applied.
